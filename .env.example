LEONARDO_API_KEY=your_leonardo_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=your_database_api_key_here"
PULSE_API_KEY="your_pulse_api_key_here"

# ========================================
# ADMIN AUTHENTICATION - SECURITY CRITICAL
# ========================================
# ⚠️ WARNING: These are example values only!
# DO NOT use these values in production!
#
# For LOCAL DEVELOPMENT:
# - These weak defaults are acceptable for testing
#
# For PRODUCTION (Netlify):
# - ADMIN_USERNAME: Choose a non-obvious username
# - ADMIN_PASSWORD: Minimum 12 characters, strong password
# - JWT_SECRET: Generate with: openssl rand -base64 32
#
# The application will REJECT weak passwords in production mode.

ADMIN_USERNAME=admin
ADMIN_PASSWORD=changeme-local-dev-only
JWT_SECRET=generate-with-openssl-rand-base64-32-min-32-chars-required

# Zoho Mail Configuration
# Step 1: Required for initial token generation
ZOHO_CLIENT_ID=your_zoho_client_id_here
ZOHO_CLIENT_SECRET=your_zoho_client_secret_here
ZOHO_USERNAME=your_zoho_email_here
ZOHO_PASSWORD=your_zoho_password_here

# Step 2: After running get-zoho-token script
ZOHO_REFRESH_TOKEN=your_zoho_refresh_token_here  # Generated automatically
ZOHO_MAIL_FROM_EMAIL=your_zoho_email_here        # Same as ZOHO_USERNAME
ZOHO_ACCOUNT_ID=your_zoho_account_id_here        # Get from Zoho Mail API settings (https://mail.zoho.com/api)

# Note: After getting the refresh token, you can remove ZOHO_USERNAME and ZOHO_PASSWORD
